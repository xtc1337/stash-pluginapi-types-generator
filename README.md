## WIP

Stash PluginApi Type Generator

This is a wip and most of the code was a bit redundant in the final solution. WIll update README with more context soon.

```bash
yarn && yarn stash:init && yarn gen-types
```
